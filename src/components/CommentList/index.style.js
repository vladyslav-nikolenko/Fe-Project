import styled from 'styled-components';

export const CommentListStyled = styled.ul`
  padding: 10px 0;
  margin: 0;
  list-style: none;
`;

export const CommentListTitle = styled.h2`
  font-weight: 600;
  font-size: var(--font-size-300);
  color: var(--color-blue-300);
`;
